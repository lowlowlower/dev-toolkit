# 📦 部署指南

## 本地开发部署

### 前提条件
- Node.js 18+ 
- npm 或 pnpm
- Docker Desktop（仅完整模式需要）

### 步骤

#### 1. 克隆项目
```bash
git clone <repository-url>
cd 环境营造
```

#### 2. 安装依赖
```bash
npm install
```

#### 3. 选择运行模式

**模拟数据模式（推荐初学者）**
```powershell
.\启动项目.ps1
# 选择选项 1
```

**完整Docker模式**
```powershell
.\启动项目.ps1
# 选择选项 2
```

## 生产环境部署

### 方案1: Vercel部署（推荐）

1. 在项目根目录创建 `vercel.json`:
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

2. 连接GitHub仓库到Vercel

3. 设置环境变量:
```
VITE_USE_MOCK=true
```

### 方案2: Netlify部署

1. 构建命令: `npm run build`
2. 发布目录: `dist`
3. 环境变量同上

### 方案3: Docker部署

1. 构建镜像:
```bash
docker build -t business-builder .
```

2. 运行容器:
```bash
docker run -p 3000:3000 business-builder
```

## 数据库部署

### 使用Supabase云服务

1. 访问 [supabase.com](https://supabase.com)
2. 创建新项目
3. 在SQL编辑器中运行 `supabase/init.sql`
4. 获取项目URL和anon key
5. 更新环境变量:
```env
VITE_USE_MOCK=false
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 自托管Supabase

使用提供的 `docker-compose.yml`:
```bash
docker-compose up -d
```

## 性能优化建议

1. **启用Gzip压缩**
2. **CDN加速** - 使用Cloudflare等CDN
3. **图片优化** - 使用WebP格式
4. **代码分割** - Vite自动处理
5. **缓存策略** - 合理设置HTTP缓存头

## 监控和日志

建议集成:
- **Sentry** - 错误追踪
- **Google Analytics** - 用户分析
- **LogRocket** - 会话回放

## 备份策略

### 数据库备份
```bash
# 导出数据
docker exec supabase-db pg_dump -U postgres > backup.sql

# 恢复数据
cat backup.sql | docker exec -i supabase-db psql -U postgres
```

### 自动备份脚本
```powershell
# 创建定时任务，每天备份一次
$trigger = New-ScheduledTaskTrigger -Daily -At 2am
$action = New-ScheduledTaskAction -Execute "PowerShell.exe" -Argument "-File backup.ps1"
Register-ScheduledTask -TaskName "BusinessBuilderBackup" -Trigger $trigger -Action $action
```

## 故障排查

### 常见问题

**Q: Docker容器启动失败**
```bash
# 检查日志
docker-compose logs

# 重新构建
docker-compose down -v
docker-compose up -d --build
```

**Q: 前端无法连接数据库**
- 检查环境变量是否正确
- 确认Supabase服务是否运行
- 检查网络连接

**Q: 卡片位置丢失**
- 检查localStorage是否被清除
- 确认数据库连接正常

## 安全建议

1. **生产环境不要使用demo密钥**
2. **启用HTTPS**
3. **配置CORS白名单**
4. **定期更新依赖**
5. **使用环境变量管理敏感信息**

## 联系支持

遇到问题？
- 提交 GitHub Issue
- 查看项目文档
- 加入社区讨论

---

**祝部署顺利！** 🚀


